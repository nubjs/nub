/* The exact text the homepage "Copy agent prompt" button copies. Deliberately
   minimal: start.md is self-contained (identity, install, cheat sheet, the
   investigate → opt-in plan → approval flow), so the prompt just points there —
   anything duplicated here would drift. */
export const START_PROMPT = `Read https://nubjs.com/start.md and follow the steps. Change nothing in my project without my approval.`;
