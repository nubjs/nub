//! Registry metadata lookup for PM provisioning (dynamic version resolution,
//! tarball URL + sha512 integrity). Reuses `workspace::scripts::npmrc_value` for
//! registry/auth config.
//
// P1 — filled in the provisioning phase.
