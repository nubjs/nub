# Known-red nub MUTATION scenarios: "<fixture> <leg> <reason>".
# This list must SHRINK. A listed scenario that passes fails the run
# (XPASS-STALE), so the fix and the entry's deletion land in one commit.
#
# A red here is a write-path divergence the differential caught: nub's mutated
# lockfile or manifest does not match what real pnpm produces for the same
# mutation. Each entry names the divergence precisely enough to act on.
