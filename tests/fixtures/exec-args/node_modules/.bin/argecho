#!/usr/bin/env node
// A node_modules/.bin entry that echoes the args it was given, so the test can
// prove `nub exec argecho --version …` forwards the flags to the bin instead of
// nub consuming them. (Node-shebang bin → nub runs it via `node <path>`, so no
// executable bit is needed for the committed fixture.)
console.log("ARGS:" + JSON.stringify(process.argv.slice(2)));
