# Known-red drop-in conformance scenarios: "<fixture> <direction> <reason>".
# Same discipline as tests/lockfile-conformance/expected-failures.txt — this list
# must SHRINK. A listed scenario that passes fails the run (XPASS-STALE), so the
# fix and the entry's deletion land in one commit.
#
# A permanent ecosystem-level impossibility does NOT belong here: no lockfile nub
# could write would clear it, so it is a skip_reason() in run.sh instead.
