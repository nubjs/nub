# Known-NON-ZERO / expected-red list for the command×flag conformance harness.
#
# Format:  <id>  <reason...>
#
# A cell listed here is EXPECTED to exit non-zero (or be a known leak) — the
# harness reports it RED(expected) and stays green overall. The moment a listed
# cell starts passing (exit 0, no leak), it's flagged XPASS-STALE and the run
# FAILS, so a behavior change can't land silently. Delete the entry when the
# cell's expected outcome changes.
#
# Tuned to the zod fixture described in README.md (commit c7ec94d3, pin removed,
# strictDepBuilds: false), a pnpm project. Every reason was re-derived by running
# the same cell against pnpm 12.4.1 (REF=1, or by hand where the cell has no
# parity verb).
#
#  (A) CORRECT non-zero — the command did its job, and pnpm 12.4.1 exits
#      non-zero on the same tree. NOT bugs.
#  (B) REAL product divergences from pnpm 12.4.1 — kept red until fixed.
#
# ── (A) correct non-zero on the zod fixture (NOT bugs) ───────────────────────
audit            vulnerabilities present in the tree → exit 1; pnpm 12.4.1 exits 1
audit-json       same, --json
audit-dev        same, --dev
audit-level      same, --audit-level high
outdated         outdated deps present → exit 1; pnpm 12.4.1 exits 1
peers-check      unmet peer (typia wants typescript <5.10.0) → exit 1; pnpm 12.4.1 prints the same issue and exits 1
run-missing-script  missing script → exit 1; pnpm 12.4.1 exits 1 (ERR_PNPM_NO_SCRIPT)
remove-dep       left-pad is not a dependency → ERR_PNPM_CANNOT_REMOVE_MISSING_DEPS; pnpm 12.4.1 exits 1 the same way
import           no npm/yarn lockfile to import → ERR_PNPM_LOCKFILE_NOT_FOUND; pnpm 12.4.1 exits 1
install-prod     root `prepare` runs husky, a devDependency --prod omits → exit 127 from the script; pnpm 12.4.1 fails the same way
pm-which         the prepared fixture declares no package manager → exit 1 (nub's own verb; no pnpm counterpart)
store-status     ERR_PNPM_MODIFIED_DEPENDENCY after install; pnpm 12.4.1 reports the same after its own install and exits 1
#
# ── (B) product divergences from pnpm 12.4.1 on this fixture ─────────────────
install          DIVERGENCE: nub's augmented resolver honors tsconfig `customConditions: ["@zod/source"]` inside lifecycle scripts, so `packages/docs postinstall` (fumadocs-mdx) resolves zod@3.25.68's `@zod/source` export to an unpublished ./index.ts and fails ERR_MODULE_NOT_FOUND; pnpm 12.4.1 and `NODE_COMPAT=1 nub install` run it to "Done" and exit 0
install-frozen   same postinstall divergence (nub=1, pnpm 12.4.1=0)
install-offline  same postinstall divergence
ci               same postinstall divergence
dedupe           same postinstall divergence (nub=1, pnpm 12.4.1=0)
prune            same postinstall divergence (nub=1, pnpm 12.4.1=0)
