{
  "name": "conform-postinstall",
  "version": "1.0.0",
  "private": true,
  "description": "Drop-in conformance fixture: a dependency with a postinstall script. @parcel/watcher downloads a prebuilt binary in its postinstall, so the fixture decides the build true in allowBuilds and both sides must run the script and leave a functional node_modules.",
  "dependencies": {
    "@parcel/watcher": "^2.5.1"
  }
}
