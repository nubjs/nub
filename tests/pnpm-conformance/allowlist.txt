# Known failures of pnpm 12.4.1's CLI suite (pnpm/crates/cli/tests/suite) under nub.
# GENERATED by gen-allowlist.mjs from a full run; regenerate rather than edit.
# Format: <module>::<test>  # <category>: <reason>

# environment (1)
side_effects_cache::a_build_with_nothing_to_restore_runs_on_every_install  # environment: fails against pnpm 12.4.1 itself on the same runner (CONF_BASELINE=1)

# bug/pre-verb-reporter-ndjson (1)
verify_deps_before_run::ndjson_exec_keeps_verifier_output_machine_readable  # bug/pre-verb-reporter-ndjson: `nub --reporter=ndjson <verb>` is rejected by nub's global option parser (default|append-only|silent); pnpm accepts ndjson before the verb, and nub accepts it after

# seam/global-shims (31)
global::global_shims_all_prefers_local_bins  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global::global_shims_auto_writes_native_dispatcher_for_node_runtime  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::a_shim_without_a_project_target_reports_it  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::a_shim_without_a_readable_target_fails  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::adding_a_shim_migrates_the_legacy_shim_for_the_same_package  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::adding_a_shim_over_another_package_s_bin_is_refused  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::adding_a_shim_under_a_global_disable_is_refused  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::adding_a_shim_under_a_higher_precedence_disable_is_refused  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::always_policy_switches_without_a_prompt  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::ancestors_of_the_pnpm_home_cannot_set_the_mode  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::bypass_env_skips_the_project_bin  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::global_fallback_prefers_the_sibling_interpreter  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::global_fallback_preserves_quoted_shebang_arguments  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::global_home_setting_off_disables_dispatch  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::global_shims_env_override_disables_dispatch  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::global_shims_setting_off_disables_dispatch  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::legacy_dispatcher_rejects_a_shim_from_another_directory  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::legacy_shim_dispatches_without_waiting_for_the_migration_lock  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::legacy_shim_launch_dispatches_and_migrates_the_bin_dir  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::lookalike_package_does_not_shadow_the_global_bin  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::malformed_trusted_settings_disable_dispatch  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::missing_global_target_reports_not_found  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::native_shim_preserves_non_shell_identifier_environment_variables  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::runtime_pin_downloads_node_on_demand  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::shim_args_reach_the_target  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::shims_can_be_added_and_removed_for_a_package_that_is_not_installed  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::trusted_project_local_bin_wins  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::unlisted_packages_do_not_switch_by_default  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::untrusted_project_falls_back_to_the_global_target  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
global_shims::verified_lockfile_does_not_skip_the_trust_gate  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these
pnpx_alias::launched_as_pnpx_injects_the_dlx_subcommand  # seam/global-shims: pnpm writes context-aware shims by copying its own executable and dispatching on argv0; through the seam the copy is the shell shim and nub's binary dispatches its own argv0 names, so the harness cannot observe these

# divergence/version-flag (14)
package_manager_check::a_failing_runtime_check_does_not_block_the_version_output  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
package_manager_check::dev_engines_runtime_with_on_fail_error_reports_a_node_version_mismatch  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
package_manager_check::dev_engines_runtime_with_on_fail_ignore_is_not_checked  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
package_manager_check::dev_engines_runtime_with_on_fail_warn_warns_about_a_node_version_mismatch  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
package_manager_check::runtime_on_fail_ignore_bypasses_the_manifest_on_fail  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
package_manager_check::the_runtime_on_fail_hint_can_be_followed_as_a_bare_flag  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::short_version_flag_prints_the_bare_version  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::version_flag_accepts_shamefully_hoist  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::version_flag_prints_the_bare_version  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::version_flag_records_a_pinned_package_manager_it_does_not_need_to_switch_to  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::version_flag_rejects_invalid_shamefully_hoist_value  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::version_flag_switches_to_project_package_manager_version  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::version_flag_switches_to_the_pinned_version_under_the_hoisted_node_linker  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project
version::version_flag_switches_to_the_version_a_range_pin_resolved_to  # divergence/version-flag: `--version` is answered by nub's front door with nub's own version, also in a pnpm project

# divergence/host-verb-in-pnpm-project (150)
clean::scripts::pm_clean_runs_the_builtin_from_a_workspace_subdirectory  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
create::create_accepts_shell_mode_flag  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
create::create_allow_build_before_name_is_parsed  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
create::create_converts_name_and_runs_via_dlx  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
create::create_forwards_args_to_package  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
create::create_options_after_name_are_forwarded  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
dlx::dlx_fails_when_a_package_spec_is_missing_from_the_catalog  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
dlx::dlx_ignores_an_ambient_workspace_manifest_above_the_cache_dir  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
dlx::dlx_provisions_a_package_manager_by_name  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
dlx::dlx_resolves_a_package_spec_against_a_named_caller_catalog  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
dlx::dlx_resolves_a_package_spec_against_the_callers_default_catalog  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::a_dir_selector_selects_the_project_in_that_dir  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::filter_without_recursive_flag_enters_recursive_exec  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::filtered_exec_keeps_single_command_in_foreground_process_group  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::legacy_dir_filtering_leaves_the_generated_root_exclusion_alone  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::legacy_dir_filtering_leaves_the_generated_root_inclusion_alone  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::legacy_dir_filtering_selects_the_subtree_below_the_dir  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::no_reporter_hide_prefix_labels_each_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::parallel_recursive_exec_has_no_workspace_concurrency_cap  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_bail_cancels_in_flight_processes  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_diff_selector_selects_changed_projects  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_filter_no_match_is_a_noop  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_filter_selects_only_matching_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_inherits_stdio_by_default  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_no_bail_runs_all_then_fails  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_no_bail_skips_dependents_of_a_failed_command  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_no_sort_makes_reverse_and_resume_no_ops  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_report_summary_records_every_package_status  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_respects_workspace_concurrency  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_resume_from_skips_only_the_anchors_dependencies  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_resumes_from_exactly_the_projects_that_passed_before_a_failure  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_runs_command_in_every_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
exec_recursive::recursive_exec_settings_only_workspace_enumerates_root_only  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
global_virtual_store::scripts_resolve_phantom_esm_imports_through_the_private_hoist  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
hooks::update_config_applies_to_recursive_run  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
ignore_workspace::workspace_packages_overrides_the_manifest_patterns  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::a_latest_older_than_the_running_pnpm_is_not_pinned  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::a_new_workspace_member_is_not_pinned  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::a_workspace_root_is_pinned  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::init_package_manager_off_in_the_workspace_manifest_leaves_the_manifest_unpinned  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::init_type_commonjs_leaves_the_type_field_out  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::init_type_from_the_workspace_manifest_is_honored  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::offline_pins_the_running_pnpm_without_a_lookup  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::should_create_package_json  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::the_author_license_and_version_settings_replace_the_scaffold_placeholders  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::the_pin_follows_the_registry_latest_rather_than_the_running_pnpm  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
init::the_scaffold_placeholders_stand_without_the_init_settings  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
install_state::pnp_loader_is_preloaded_for_lifecycle_run_and_exec_commands  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
install_test::install_test  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
install_test::it_alias  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
interrupt::a_parallel_run_relays_the_interrupt_to_every_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
lifecycle_scripts::project_scripts::stamps_the_configured_user_agent_on_install_scripts_run_and_exec  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
lockfile_only::local_overrides::exec_content_check_accepts_a_link_override_from_a_custom_lockfile_dir  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
lockfile_only::local_overrides::exec_content_check_accepts_a_root_relative_link_override  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
package_manager_check::a_command_outside_the_install_family_records_the_pinned_package_manager  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
python::installs_node_cargo_and_python_through_the_real_coordinator  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
python::installs_real_environment_with_ranges_extras_markers_scripts_and_offline_replay  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run::run_exports_node_path_from_a_custom_virtual_store_dir  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run::run_exports_node_path_when_prefer_symlinked_executables  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run::shell_emulator::without_the_setting_the_same_project_cannot_spawn_its_shell  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::aggregate_output_keeps_each_project_in_one_block  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::depends_on_cycle_is_an_error  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::depends_on_runs_the_tasks_a_task_depends_on_in_dependency_order  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::dry_run_json_emits_the_tasks_and_their_resolved_edges  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::dry_run_outside_a_recursive_run_is_an_error  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::dry_run_prints_one_stable_linearization_and_runs_nothing  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::explicitly_empty_depends_on_starts_without_waiting  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::failed_upstream_task_is_reported_as_the_failure_not_a_missing_script  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::filter_without_recursive_flag_enters_recursive_run  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::filtered_run_keeps_single_script_in_foreground_process_group  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::filtered_run_prints_the_script_command_unless_silent  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::filtered_run_without_script_name_lists_selected_and_root_scripts  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::ignore_workspace_cycles_downgrades_the_task_cycle_error_to_a_warning  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::include_workspace_root_setting_is_read_from_the_workspace_manifest  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::missing_requested_script_errors_before_upstream_tasks_run  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::missing_script_is_reported_skipped_and_does_not_sever_the_chain  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::no_bail_skips_dependents_of_a_failed_task_and_runs_unrelated_ones  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::parallel_before_run_starts_selected_projects_concurrently  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::parallel_implies_stream  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_lifecycle_aliases_use_recursive_run_options  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_bail_cancels_in_flight_processes  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_bail_cancels_in_flight_shell_emulator_tasks  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_bail_without_report_summary_writes_no_file  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_bail_writes_summary_then_stops_at_first_failure  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_diff_selector_selects_changed_projects  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_does_not_follow_bare_semver_deps_as_workspace_edges  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_does_not_persist_a_task_skipped_by_the_recursion_guard  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_errors_when_no_package_has_the_script  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_exclude_filter_skips_excluded_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_executes_every_script_matching_a_regexp_selector  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_executes_script_in_every_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_filter_no_match_is_a_noop  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_filter_no_matching_script_reports_no_selected_packages  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_filter_prod_all_exclusion_also_drops_root  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_filter_prod_follows_production_deps_only  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_filter_selects_only_matching_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_filters_hidden_regexp_matches_when_a_visible_script_matches  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_finds_workspace_root_bin_on_path  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_if_present_is_a_noop_when_no_package_has_the_script  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_inherits_stdio_without_stream  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_keeps_a_failure_when_a_later_selected_script_passes  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_keeps_a_pass_when_a_later_matching_script_is_empty  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_missing_hidden_script_reports_no_script_not_hidden  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_mixed_filter_runs_prod_selected_before_regular  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_no_sort_makes_reverse_and_resume_no_ops  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_no_sort_uses_workspace_order  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_pipes_stdio_when_tasks_can_interleave  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_prefers_project_bin_over_workspace_root_bin  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_preloads_each_project_pnp_loader  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_reads_bail_from_workspace_config  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_reads_reverse_from_workspace_config  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_reads_sort_from_workspace_config  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_recursion_guard_skips_originating_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_rejects_hidden_script_name  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_report_summary_records_every_package_status  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_resolves_local_bin_on_path_per_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_respects_task_concurrency  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_respects_workspace_concurrency  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_resume_from_starts_at_the_given_package  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_resume_from_unknown_package_errors  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_resumes_from_exactly_the_tasks_that_passed_before_a_failure  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_runs_pre_and_post_when_enabled  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_skips_empty_script_body  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_without_script_name_errors_with_script_name_is_required  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_workspace_root_adds_the_root_to_a_filter_selection  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_run_workspace_root_selects_only_the_root_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::recursive_top_level_if_present_is_a_noop_when_no_package_has_the_script  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::regexp_selected_empty_script_errors_before_upstream_tasks_run  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::reporter_hide_prefix_drops_the_prefix_from_streamed_script_output  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::stream_prefixes_recursive_script_output_with_the_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::streamed_output_survives_non_utf8_bytes  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::task_concurrency_of_one_keeps_scripts_in_the_foreground_process_group  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::task_starts_as_soon_as_its_dependencies_finish  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::test_pattern_from_workspace_yaml_is_respected_by_the_test_script  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::top_level_fallback_does_not_exec_local_bin_recursively  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::top_level_fallback_enters_recursive_run  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::use_stderr_diverts_reporter_output  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
run_recursive::workspace_dependency_cycle_is_an_error_naming_the_participating_tasks  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
self_update::self_update_loads_config_and_reaches_the_resolver  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
sync_injected_deps_after_scripts::a_listed_script_refreshes_every_injected_copy  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
sync_injected_deps_after_scripts::a_listed_script_removes_the_link_of_a_bin_it_dropped  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
update_notifier::the_other_commands_on_the_install_pipeline_do_not_check  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
verify_deps_before_run::a_shared_lockfile_is_checked_from_a_directory_without_a_manifest  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
verify_deps_before_run::dedupe_peers_lockfile_regeneration_installs_before_running_the_script  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
verify_deps_before_run::exec_keeps_verifier_output_out_of_child_stdout  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
verify_deps_before_run::install_action_reruns_a_production_only_install  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
verify_deps_before_run::separate_lockfiles_check_only_the_active_workspace_project  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
verify_deps_before_run::silent_recursive_exec_suppresses_verifier_output  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
verify_deps_before_run::silent_recursive_run_suppresses_verifier_output  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's
version::child_pnpm_selects_the_version_for_its_own_directory  # divergence/host-verb-in-pnpm-project: the command line reaches nub's own command (run/exec/dlx/create/init/env/self-update/test/start/stop/restart/install-test, a pre-verb pnpm flag, or pnpm's bare-name fallback) although the project is pnpm's

# identity/nub-project (186)
add::add_accepts_multiple_local_package_selectors  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
approve_builds::approve_builds_all_with_args_is_rejected  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
approve_builds::approve_builds_rejects_an_argument_that_names_no_package  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
audit::audit_rejects_unknown_subcommands  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
audit::audit_signatures_rejects_extra_subcommand_argument  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
bugs::fails_when_no_bugs_url_can_be_derived  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
bugs::fails_when_no_package_json_exists  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
bugs::fails_when_registry_package_has_no_bugs_url  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
ci_frozen_lockfile::ci_install_without_a_nonempty_lockfile_generates_one  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
clean::clean_removes_package_links_into_the_virtual_store  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
clean::clean_removes_packages_and_pnpm_entries_but_preserves_non_pnpm_dotfiles  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
clean::scripts::pm_clean_runs_the_builtin_when_a_clean_script_exists  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
config_get_set::config_set_checks_the_package_manager_when_writing_project_configuration  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
create::create_errors_when_no_name_given  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
deprecate::fails_on_unauthorized  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
deprecate::fails_when_message_is_not_provided  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
deprecate::fails_when_package_is_not_provided  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
deprecate::fails_with_no_matching_versions_for_an_invalid_range  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::add_rejects_non_exact_semver_versions  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::add_rejects_unsafe_web_otp_challenge_urls  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::add_rejects_web_otp_challenge_urls_with_control_chars  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::add_reports_web_otp_challenge  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::add_sanitizes_and_truncates_registry_error_body  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::ls_fails_when_package_is_missing  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::ls_redacts_registry_credentials_on_network_error  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::ls_rejects_oversized_dist_tags_response  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::rm_fails_when_tag_is_missing  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dist_tag::rm_refuses_latest  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
dlx::dlx_errors_when_no_command_given  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
env::a_bare_env_asks_for_a_subcommand  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
env::an_unknown_subcommand_is_rejected  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
env::use_without_global_is_refused_after_the_deprecation_warning  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
exec::exec_errors_when_command_not_found  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
exec::exec_errors_when_no_command_given  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
exec::exec_preserves_a_detached_process_after_success  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
exec::exec_propagates_nonzero_exit_code  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
exec::exec_shell_mode_preserves_embedded_quotes  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
exec::exec_shell_mode_runs_shell_command  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
exec::exec_stamps_pnpm_package_name_from_manifest  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
global::global_add_pnpm_is_rejected  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
global::global_update_pnpm_is_rejected  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
hooks::update_config_applies_to_exec  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
hooks::update_config_applies_to_rebuild  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
hooks::update_config_applies_to_run  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
hooks::update_config_omits_the_settings_nothing_set  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
hooks::update_config_sees_cli_flags_and_resolved_defaults  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
hooks::update_config_sees_npmrc_scoped_registries  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
hooks::update_config_sees_registry_credentials  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
init::an_unreachable_registry_pins_the_running_pnpm  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
init::init_scaffolds_in_the_cwd_inside_an_existing_project  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
init::no_init_package_manager_leaves_the_manifest_unpinned  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
init::should_throw_on_existing_file  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
install::migrated_keys_are_reported_by_the_up_to_date_fast_path  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
install::migrated_keys_are_reported_through_a_utf8_bom  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
install::migrated_keys_under_the_package_json_pnpm_field_are_reported  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
interrupt::a_third_interrupt_ends_pnpm_even_when_the_script_ignores_them  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
interrupt::run_ends_with_the_signal_that_killed_the_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
login::a_login_writes_a_config_the_reader_reads_back  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
login::adduser_alias_rejects_a_non_interactive_terminal  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
login::login_rejects_a_non_interactive_terminal  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
logout::logout_matches_the_registry_however_the_url_is_spelled  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
logout::logout_removes_the_token_from_config_yaml  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
logout::logout_revokes_token_and_removes_it_from_auth_ini  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
not_implemented::the_unimplemented_commands_swallow_their_arguments  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
not_implemented::the_unimplemented_npm_commands_point_at_the_npm_cli  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
pack::before_packing_hook_rewrites_the_packed_manifest  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
pack::packing_reuses_the_hooks_that_updated_config  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::a_global_command_warns_instead_of_failing_the_package_manager_check  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::a_manifest_that_is_not_an_object_fails_instead_of_panicking  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::a_project_pinned_to_another_package_manager_still_refuses_an_ordinary_add  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::a_runtime_without_a_version_range_fails  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::an_invalid_bun_version_range_names_bun  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::an_invalid_deno_version_range_names_deno  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::an_invalid_node_version_range_fails_with_the_runtime_on_fail_hint  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::control_characters_in_a_package_manager_name_are_stripped_from_the_output  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::dev_engines_package_manager_naming_another_package_manager_fails  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::engines_runtime_is_checked_too  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::install_fails_when_the_project_pins_another_package_manager  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::pm_on_fail_warn_downgrades_the_other_package_manager_failure_to_a_warning  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
package_manager_check::runtime_array_entries_are_checked_beyond_the_first_one  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
peers::standalone_install_does_not_require_catalogs_for_linked_peers  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
ping::fails_on_a_network_failure  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
ping::fails_when_the_registry_responds_with_an_error_status  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
publish::dry_run_uploads_nothing  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
repo::repo_fails_without_package_json  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
repo::repo_fails_without_repository_field  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
restart::restart_passes_args_to_scripts  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
restart::restart_runs_stop_restart_start_scripts  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
restart::restart_with_if_present_skips_missing_scripts  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
restart::start_runs_declared_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
restart::stop_runs_declared_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
restart::stop_without_script_succeeds_with_if_present  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::no_bail_reports_a_single_failed_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::no_bail_reports_failures_in_selection_order  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::regexp_selected_scripts_cancel_siblings_after_failure  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::regexp_selected_scripts_no_bail_lets_siblings_finish  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::regexp_selected_scripts_no_bail_runs_every_script_sequentially  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_empty_start_script_hits_server_js_guard  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_executes_every_script_matching_a_regexp_selector  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_failing_test_script_prints_test_failed_message  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_forwards_the_separator_and_option_shaped_arguments  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_lists_scripts_when_no_name_given  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_rejects_regexp_flags_in_a_selector  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_start_fallback_uses_dir_for_server_js_probe  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_start_falls_back_to_node_server_js_when_present  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::run_start_without_script_or_server_errors  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::script_shortcuts_forward_every_argument_to_the_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::top_level_fallback_forwards_dotted_config_args_to_local_bin  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::top_level_fallback_runs_local_bin_when_script_is_missing  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::top_level_fallback_runs_local_bin_without_package_json  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::top_level_fallback_runs_package_yaml_script_with_dir  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::top_level_fallback_runs_script_before_local_bin  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::top_level_fallback_without_manifest_does_not_attempt_an_install  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
run::top_level_if_present_is_a_noop_for_missing_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
runtime::runtime_unknown_subcommand_runs_with_default_ndjson_and_silent_reporters  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
set_script::fails_when_arguments_are_missing  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
set_script::fails_when_no_arguments_are_given  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
set_script::rejects_unsafe_script_names  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::a_missing_subcommand_is_rejected_with_the_stage_error_code  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::an_unknown_subcommand_is_rejected_with_the_stage_error_code  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::approve_maps_a_web_auth_challenge_to_the_non_interactive_error  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::approve_rejects_a_stage_id_that_is_not_a_uuid  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::approve_rejects_duplicate_package_versions_before_approving_the_batch  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::approve_surfaces_a_plain_401_as_a_stage_registry_error  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::approve_without_a_stage_id_requires_an_interactive_terminal  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::download_rejects_traversal_through_the_tarball_manifest_name  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::download_rejects_traversal_through_the_tarball_manifest_version  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::list_rejects_version_specifiers  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::publish_dry_run_reports_that_the_package_would_be_staged  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::stage_reject_against_pnpr_deletes_the_staged_publish  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stage::view_requires_a_uuid_stage_id  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
star_tests::star_legacy_path_reports_package_not_found  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
star_tests::star_registry_error_when_both_endpoints_fail  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
star_tests::star_unauthorized  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stars_tests::stars_other_user_registry_error  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stars_tests::stars_unauthorized_without_username  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
stars_tests::stars_user_not_found  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
store::empty_store_dir_override_restores_the_platform_default  # identity/nub-project: the test's first `store path` runs where no pnpm marker exists, so the default it compares against is nub's store
test_command::test_runs_declared_test_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
test_command::test_skips_missing_test_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
undeprecate::fails_when_message_is_provided  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
undeprecate::fails_when_not_deprecated  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
undeprecate::fails_when_package_is_not_provided  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::a_405_on_a_full_unpublish_reports_unpublish_forbidden  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::a_classic_otp_challenge_without_a_terminal_reports_otp_non_interactive  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::a_range_matching_every_version_is_a_full_unpublish  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::a_web_auth_challenge_without_a_terminal_reports_otp_non_interactive  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::an_unauthorized_delete_reports_unauthorized  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::fails_when_no_versions_match_the_range  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::fails_when_package_is_not_found  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::fails_when_package_is_not_provided  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::fails_when_the_package_has_no_versions  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unpublish::refuses_a_full_unpublish_without_force  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unstar_tests::unstar_registry_error_when_both_endpoints_fail  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
unstar_tests::unstar_unauthorized  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::default_install_action_installs_before_running_the_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::empty_env_value_disables_the_gate  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::env_var_outranks_the_cli_config_override  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::error_action_follows_the_dependency_state  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::exec_children_get_the_check_disabled_through_their_env  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::exec_runs_the_gate_too  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::false_disables_the_gate  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::prompt_action_errors_when_not_interactive  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::scripts_get_the_check_disabled_through_their_env  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::silent_exec_suppresses_verifier_output  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::unrecognized_env_value_checks_without_acting  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
verify_deps_before_run::warn_action_warns_and_runs_the_script  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::from_git_fails_when_no_matching_tag_exists  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::from_git_rejects_a_malformed_version_tag  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::invalid_bump_type_fails  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::invalid_manifest_version_fails  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::manifest_without_name_or_version_has_no_packages_to_version  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::missing_bump_without_recursive_fails  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::same_version_fails_unless_allowed  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
version::unclean_working_tree_fails_unless_git_checks_are_disabled  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
view::fails_without_package_name_or_manifest  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
view::manifest_without_name_is_invalid_package_json  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
view::no_matching_version_errors  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
view::non_object_manifest_is_invalid_package_json  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
view::package_not_found_is_fetch_404  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
view::rejects_non_registry_spec  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
whoami::fails_when_not_logged_in  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
whoami::fails_when_the_registry_rejects_the_request  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
with::with_current_bypasses_the_package_manager_check_when_an_unrelated_manager_is_pinned  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
yarn_workspaces_field::a_repeat_install_taking_the_up_to_date_path_warns_too  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
yarn_workspaces_field::an_install_warns_about_a_workspaces_field_with_no_pnpm_workspace_yaml  # identity/nub-project: the fixture writes no pnpm marker, so nub applies its own project identity (its verbs, lockfile, configuration and ERR_NUB_* codes)
