# Allowlist of KNOWN divergences in pnpm's own black-box suite run against nub.
#
# Each non-comment line is a substring matched against a failing jest test's
# full name ("<describe> > <it>" as jest reports it, or a bare test-file path).
# A failure whose name matches ANY line here is reported as KNOWN-FAILING (not a
# surprise) and does not fail the run. A failure matching NO line is a SURPRISE
# and fails the run. Keep this list SHORT and each entry justified — a stale
# entry (a test that now passes but is still allowlisted) is flagged on a full
# run, so this file stays honest and shrinks as bugs are fixed.
#
# IMPORTANT: every entry below was verified to MATCH a real failing test in a
# full-suite run (pnpm v10.15.1, 2026-06-20). Do not add speculative entries —
# an entry that matches nothing fails the run as STALE. When you fix the
# underlying divergence, remove its line here.
#
# Format:  one match-substring per line; blank lines and #-comments ignored.

# ── Global install / link / bin family (~13 tests) ───────────────────────────
# nub uses its own global package layout (<pnpm-home>/global-nub) and does not
# replicate pnpm's global install/link/uninstall/bin/-g surface. This is a
# global-install design area the maintainer owns — allowlisted as a known
# divergence cluster pending a product call (NOT auto-changed). Tracked for the
# `pnpm-compat-harness-bugs` thread to triage which (if any) should converge.
pnpm root -g
pnpm bin -g
global installation
run lifecycle events of global packages
global update to latest
global update should not crash if there are no global packages
link globally the command of a package
link a package globally without specifying the global option
link a package from a workspace to the global package
uninstall global package with its bin files

# ── packageManager-field version management ──────────────────────────────────
# nub does not replicate pnpm's manage-package-manager-versions / version-switch
# behavior (switching to / refusing the pnpm version named in packageManager,
# self-update editing the field). One path leaks nub-identity text where pnpm
# emits "This project is configured to use yarn" — a real finding to triage.
do not switch to the pnpm version specified
do not switch to pnpm version that is specified
throws error if pnpm tools dir is corrupt
install should not fail if the used pnpm version does not satisfy
install should fail if the project requires a different package manager
install should not fail for packageManager field with hash
some commands should not fail if the required package manager is not pnpm
self-update updates the packageManager field

# ── Per-package node version ─────────────────────────────────────────────────
# nub does not honor a per-package node version on a recursive run.
recursive run when some packages define different node versions

# ── Misc front-door divergences (triage) ────────────────────────────────────
# `pnpm` in an empty dir creating package.json; dlx reading .npmrc registry.
# Both real divergences surfaced 2026-06-20; kept allowlisted until triaged.
create a package.json if there is none
dlx read registry from .npmrc in the current directory
